# 火山方舟真实模型接入（示例；复制为 .env 并填入真实 Key）
# 未配置 ARK_API_KEY 或显式 AI_PROVIDER=mock 时，后端使用本地 mock
AI_PROVIDER=ark
ARK_API_KEY=your-ark-api-key
ARK_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
ARK_CHAT_MODEL=doubao-seed-2-0-lite-260215
# 识花专用模型（可选，缺省同 ARK_CHAT_MODEL；如需识花与科普文案用不同模型可单独设置）
# ARK_VLM_MODEL=
# 视频识花（广义的花）属性抽取专用模型（可选，缺省同 ARK_VLM_MODEL；视频链路对速度敏感可换极速模型）
# ARK_VIDEO_MODEL=
ARK_IMAGE_MODEL=doubao-seedream-5-0-260128
# 静态资源公网前缀（仅生产部署需要）：后端公网地址，如 https://flowers-api-xxxx.onrender.com
# 前端与 API 不同域时必须配置，否则生成图/上传图会被解析到前端域名下 404
# PUBLIC_BASE_URL=
